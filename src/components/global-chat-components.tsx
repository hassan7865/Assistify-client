"use client";

import React from 'react';

const GlobalChatComponents: React.FC = () => {
  // Note: This component is temporarily disabled as the referenced components were deleted
  // when the dashboard route was changed to chat. The components will need to be recreated
  // in the new chat structure if needed.
  
  return null;
};

export default GlobalChatComponents;
